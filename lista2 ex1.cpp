#include <iostream>
using namespace std;

int main() {
    int x, y, z;
    
    x = 7 + 3 * 6 / 2 - 1;
    y = 2 % 2 + 2 * 2 / 2;
    z = (3 * 9 * (3 + (9 * 3 / (3))));
    
    cout << "Valor de x: " << x << endl;
    cout << "Valor de y: " << y << endl;
    cout << "Valor de z: " << z << endl;
    
    return 0;
}
